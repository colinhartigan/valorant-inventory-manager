import {Dialog, DialogTitle, DialogContent, DialogActions, List, ListItem, ListItemAvatar, ListItemButton, ListItemText} from '@mui/material';

function ProfileEdit(props){


    return (
        <Dialog open={true} maxWidth={"md"}>
            <DialogTitle>Collection profiles</DialogTitle>
            <DialogContent>
                <List>
                    <ListItem>
                        
                    </ListItem>
                </List>
            </DialogContent>
        </Dialog>
    )
}

export default ProfileEdit;